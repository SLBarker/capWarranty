_schema-version: 3.3.0
ID: capWarranty
description: A simple CAP project.
version: 1.0.0
modules:
- name: capWarranty-srv
  type: nodejs
  path: gen/srv
  requires:
  - name: capWarranty-auth
  - name: capWarranty-db
  provides:
  - name: srv-api
    properties:
      srv-url: ${default-url}
  parameters:
    buildpack: nodejs_buildpack
    readiness-health-check-http-endpoint: /health
    readiness-health-check-type: http
  build-parameters:
    builder: npm
- name: capWarranty-db-deployer
  type: hdb
  path: gen/db
  requires:
  - name: capWarranty-db
  parameters:
    buildpack: nodejs_buildpack
- name: capWarranty
  type: approuter.nodejs
  path: app/router
  requires:
  - name: srv-api
    group: destinations
    properties:
      forwardAuthToken: true
      name: srv-api
      url: ~{srv-url}
  - name: capWarranty-auth
  - name: capWarranty-destination-service
    group: destinations
    properties:
      forwardAuthToken: false
      name: ui5
      url: https://ui5.sap.com
  parameters:
    disk-quota: 256M
    keep-existing-routes: true
    memory: 256M
- name: capWarranty-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: capWarranty-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - html5warranty.zip
      name: html5warranty
      target-path: resources/
- name: html5warranty
  type: html5
  path: app/html5warranty
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: capWarranty-auth
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      tenant-mode: dedicated
      xsappname: capWarranty-${org}-${space}
    path: ./xs-security.json
    service: xsuaa
    service-plan: application
- name: capWarranty-db
  type: com.sap.xs.hdi-container
  parameters:
    service: hana
    service-plan: hdi-shared
- name: capWarranty-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: capWarranty-html5-srv
    service-plan: app-host
- name: capWarranty-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: false
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: capWarranty-destination-service
    service-plan: lite
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
  - builder: custom
    commands:
    - npm ci
    - npx cds build --production
